On development machine:
rm -rf HotLive-Server/node_modules
tar -zcf HotLive-Server.tgz HotLive-Server/
scp HotLive-Server.tgz root@120.26.118.194:/root/

On Host server:

tar -zxvf HotLive-Server.tgz 
cd HotLive-Server/
npm install
vi config.json and modify port to 80, disable log.

forever list
forever stop (pid)
forever start server.js
